// 정수 1개 입력받은 후, 나누어 출력하기!!
//
#include<stdio.h>

int main() {
  int a; int b; int c; int d; int e;

  scanf("%1d%1d%1d%1d%1d", &a, &b, &c, &d, &e);
  printf("[%d]\n[%d]\n[%d]\n[%d]\n[%d]\n", a*10000, b*1000, c*100, d*10, e);
  return 0;
}
